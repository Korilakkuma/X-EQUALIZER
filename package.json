{
  "name": "x-equalizer",
  "version": "0.0.0",
  "engines": {
    "node": ">=22.0.0 <=24.14.x"
  },
  "description": "Chrome extension and Firefox addon for Graphic Equalizer",
  "scripts": {
    "clean": "rm -rf dist",
    "eslint": "eslint 'src/**/*.ts'",
    "stylelint": "stylelint 'src/**/*.css'",
    "fix:eslint": "eslint --fix 'src/**/*.ts'",
    "fix:stylelint": "stylelint --fix 'src/**/*.css'",
    "format:check": "prettier --check 'src/**/*.{ts,js,css}'",
    "format": "prettier --write 'src/**/*.{ts,js,css}'",
    "lint": "run-p eslint stylelint",
    "fix": "run-p fix:eslint fix:stylelint",
    "type": "tsc --noEmit",
    "build": "npm run clean && mkdir dist && cp src/main.js dist/main.js && cp src/styles/app.css dist/app.css",
    "test": "echo 'No test cases.'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "src/**/*.ts": [
      "eslint --fix",
      "prettier --write 'src/**/*.{ts,js}'"
    ],
    "src/**/*.css": [
      "stylelint --fix",
      "prettier --write 'src/**/*.css'"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Korilakkuma/X-EQUALIZER.git"
  },
  "keywords": [
    "Chrome extension",
    "Firefox addon",
    "Graphic Equalizer",
    "Web Audio API",
    "XSound"
  ],
  "author": "Korilakkuma <rilakkuma.san.xjapan@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Korilakkuma/X-EQUALIZER/issues"
  },
  "homepage": "https://github.com/Korilakkuma/X-EQUALIZER",
  "dependencies": {
    "xsound": "^4.2.7"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/chrome": "^0.1.40",
    "@types/firefox-webext-browser": "^143.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.6.0",
    "eslint": "^10.2.1",
    "globals": "^17.5.0",
    "husky": "4.3.8",
    "lint-staged": "^16.4.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.8.3",
    "stylelint": "^17.8.0",
    "stylelint-config-recess-order": "^7.7.0",
    "stylelint-config-standard": "^40.0.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.58.2"
  }
}
